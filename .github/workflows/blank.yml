name: Scheduled Workflow

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show timestamp
        run: echo "Workflow triggered at $(date -u)"
